import { Meta, Canvas, Story } from '@storybook/addon-docs';
import { I18nProvider } from '@i18n/context/I18nContext.tsx'
import { graphicalNo as no, graphicalYes as yes } from '/docs/consts.ts'

import * as ExpandableStories from '../Expandable.stories.tsx'

export const LinkToRootDocs = () => <p><b><a href="/about-docodylus--docs&active-tab=i18n" target="_blank">Learn more about Internationalization in Docodylus</a></b></p>

**Expandable** provides default text translations with the use of an additional **I18nProvider**.
 To see translations for the supported languages, use the locale selector in the Storybook toolbar at the top of the screen.

 In the absence of the `I18nProvider` component, Expandable renders the default text in American English.

{ /* Learn more about internationalization in Docodylus on the [root documentation page][root-docs] */ }

 ### Configurable strings
 | property                                                     | default value (en) | pluralization | interpolation |
 |--------------------------------------------------------------|--------------------|---------------|---------------|
 | **iaindavis-dev.docodylus.layout.expandable.expandPrompt**   | "show more..."     | { no }        | { no }        |
 | **iaindavis-dev.docodylus.layout.expandable.collapsePrompt** | "show less"        | { no }        | { no }        |

<LinkToRootDocs/>
    
